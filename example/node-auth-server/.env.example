# Example environment file. Real values are also hard-coded in src/config.js
# (which is itself one of the bugs the auditor should flag).
PORT=3000
JWT_SECRET=changeme-please
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=Password123!
